machine:
    python:
        version:
            2.7.6

dependencies:
    override:
        - git submodule update --init
        - pip install -r pip_requirements.txt

test:
    override:
        - python test.py
